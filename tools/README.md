# tools

Optional local tool directory.

You may copy a portable QEMU build into `tools\qemu`, but that directory is ignored by Git. QEMU is a third-party GPL-licensed component and is not part of this repository's MIT license.
